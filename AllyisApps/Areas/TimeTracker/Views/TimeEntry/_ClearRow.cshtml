@using AllyisApps.ViewModels.TimeTracker.TimeEntry;
@model DeleteTimeEntryViewModel

@using (Html.BeginForm(ActionConstants.DeleteTimeEntry, ControllerConstants.TimeEntry, FormMethod.Post))
{
	@Html.HiddenFor(m => m.TimeEntryId)

	<td><input type="submit" value=@Strings.Delete /></td>
}