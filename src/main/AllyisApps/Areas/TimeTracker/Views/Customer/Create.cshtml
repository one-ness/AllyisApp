@model AllyisApps.ViewModels.TimeTracker.Customer.EditCustomerInfoViewModel

@{
    DisplayHints.BreadcrumbNavPartialLocation = "~/Areas/TimeTracker/Views/Shared/_BreadcrumbNavPartial.cshtml";
    DisplayHints.LayoutThemeBundle = "~/Content/Timetracker";
    ViewBag.Title = AllyisApps.Resources.TimeTracker.Views.Customer.Strings.Create;
    ViewData.Add(ControllerConstants.Controller, ControllerConstants.Customer);
    ViewData.Add(ActionConstants.Action, ActionConstants.Create);
}

<div class="shrink-wrap-center">
    @{Html.RenderPartial("_CustomerEdit", Model, ViewData);}
</div>

@section Scripts {
    <script type="text/javascript">
        var preserveState = "@(Model != null ? (AllyisApps.Resources.ViewModels.Auth.States.ResourceManager.GetString(Model.Clean(Model.State)) ?? Model.State) : string.Empty)";
        var dropdownempty = "@AllyisApps.Resources.TimeTracker.Views.Customer.Strings.DropDownEmpty";
    </script>
    <script type="text/javascript" src="~/Scripts/allyis-updateStateDDL.js"></script>
}