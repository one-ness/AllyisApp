
package com.allyis.timetracker.domain.access;

import com.allyis.timetracker.domain.model.*;


/**
 * All methods in this interface should be run in a background thread
 * because they use the network.
 */
/*public interface ApiClient {
    public LoginResult Login(String username, String password);
    public RenewLoginResult RenewLogin(SessionToken token);
    public ClockResult PunchClock(SessionToken token);
    public LogoutResult Logout(SessionToken token);
}
*/